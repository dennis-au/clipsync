# Binaries
/clipsync
*.exe
*.test
*.out

# State / runtime data
/var/
items.json
blobs/

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/
.env
.env.*
!.env.example

# `compose.yaml` is an operator-owned deployment file. Start from
# `compose.example.yaml` when creating a new stack.
/compose.yaml
/DEPLOYMENT.local.md

# Encrypted deployment backups are intentionally kept outside the repository.
/backups/
/work/

# Local SwiftPM build artifacts for the macOS status utility.
/macos/ClipSyncControl/.build/
/macos/ClipSyncControl/dist/
